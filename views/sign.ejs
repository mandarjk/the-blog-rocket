<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/form.css" />
    <title>The Blog Rocket</title>
</head>
<body>
    <%- include('header'); -%>
    <section>
    	<div id="formdiv">
            <form id="formr" method="post" action="/articles/users">
                    <% if(error_msg != '') { %>
                        <p><%= error_msg %></p> 
                    <% } %>
                <h3>Sign Up / Register</h3>
                <label>Name : </label>
                <input type="text" id="fname" name="fname" placeholder="Full name">
                <label>password : </label>
                <input type="password" id="password" name="password" placeholder="Password">
                <label>email :</label>
                <input type="email" id="email" name="email" placeholder="email@xyz.com">
                <button id="signup">Sign Up</button>
    	    </form>
        </div>
        <div id="formdiv2">
            <form id="forms" method="post" action="/articles/verify">
                <% if(error != '') { %>
                    <p><%= error %></p> 
                <% } %>
                <% if(success_msg !=''){%>
                    <p id="succ"><%=success_msg %></p>
                <%}%>
                <h3>Sign In</h3>
                <label>email :</label>
                <input type="email" id="email" name="email" placeholder="email@xyz.com">
                <label>password : </label>
                <input type="password" id="password" name="password" placeholder="Password">
                <button id="signin">Sign In</button>
    	    </form>
        </div>
    </section>
</body>
</html>